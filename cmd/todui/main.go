// Command todui is a tiny, config-driven task manager: a CLI-first core
// with a Charm Bubble Tea TUI layered on top.
//
// This file is the composition root — the only place that constructs concrete
// adapters and wires them to the interface seams in internal/ports. It is a
// placeholder until the CLI and TUI drivers land (see .claude/rules).
package main

import "fmt"

func main() {
	fmt.Println("todui: under construction")
}
